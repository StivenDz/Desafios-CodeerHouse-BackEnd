{
    "version": 2,
    "routes":[
        {
            "src":"/(.*)",
            "dest":"dist/src"
        }
    ],
    "installCommand": "npm install",
    "buildCommand": "npm run build",
    "builds": [
        {
             "src": "./dist/src/index.js", "use": "@vercel/node"
        }
    ]
}